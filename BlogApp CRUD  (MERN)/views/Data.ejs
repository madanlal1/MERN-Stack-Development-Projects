<%- include('./partials/header') %>
<%- include('Navbar') %>


<!-- <% if(data!=0) { %> -->

<table class="table">
    <thead>
      <tr>
        <th scope="col">#</th>
        <th scope="col">Firstname</th>
        <th scope="col">Lastname</th>
        <th scope="col">Email</th>
        <th scope="col">Profile</th>
        <th scope="col">Update</th>
        <th scope="col">Delete</th>

      </tr>
    </thead>
    <tbody>
      <% data.forEach((row,index) => { %>

      
      <tr>
        <th scope="row"> <%= index %> </th>
        <td> <%= row.fname %> </td>
        <td> <%= row.lname %> </td>
        <td> <%= row.email %> </td>
        <td> <img src="images/<%= row.image %>" width="150px" height="150px" alt='image'/> </td>
        <td id="<%= row._id %>" ><a href="retdata/<%= row._id %>"  type="button" class="btn btn-success">Update</a></td>
        <td id="<%= row._id %>" ><a href="delete/<%= row._id %>"  type="button" class="btn btn-danger">Delete</a></td>
      </tr>

      <%} )%>

    </tbody>
  </table>

  <% } 
  else {%>
    <div>
      <p>data not found!</p>
    </div>
    <%}%>

  <%- include('./partials/footer') %>